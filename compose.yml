services:
  aios-lb:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aios_lb
    restart: unless-stopped
    ports:
      - "7035:7035"
    volumes:
      - ./config.yml:/app/config.yml:ro
